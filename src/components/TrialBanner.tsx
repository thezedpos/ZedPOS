"use client";

// We are returning null to hide this old banner and prevent the "Double Banner" issue.
// The real banner is now inside src/app/dashboard/page.tsx
export function TrialBanner() {
  return null;
}